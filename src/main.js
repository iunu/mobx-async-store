import Store from './Store'

import Model, {
  attribute,
  relatedToMany,
  relatedToOne,
  validates
} from './Model'

export {
  attribute,
  Model,
  relatedToMany,
  relatedToOne,
  Store,
  validates
}
